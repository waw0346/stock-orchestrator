# ── 중첩 git 레포 (GitHub Desktop 튜토리얼 잔재) ──────────────────────────
picks/desktop-tutorial/

# ── 트랜지언트 DART 캐시 (분기 1회 갱신, 커밋 불필요) ──────────────────────
picks/cache/*.json
picks/cache/foreign_flow_history.csv
picks/cache/*.test.csv

# ── 이상보고 대기열 (Stop hook 실행 후 자동 삭제) ───────────────────────────
picks/alerts/*.json

# ── Claude Code 개인 로컬 승인/환경 상태 (공유 불필요) ─────────────────────
.claude/settings.local.json

# ── 로컬 비밀값 / API 키 (커밋 금지) ───────────────────────────────────────
.env
.env.*
!.env.example

# ── Obsidian 개인 기록 DB (코드와 분리, git 관리 제외) ─────────────────────
obsidian/

# ── 테스트 아티팩트 (run_paper_trading_tests.ps1 이 생성/삭제) ──────────────
picks/*.test.json
picks/*.test.csv
picks/paper_price_snapshot.target.test.json

# ── Python 캐시 및 컴파일 파일 ──────────────────────────────────────────────
.venv/
__pycache__/
*.py[cod]
*$py.class
